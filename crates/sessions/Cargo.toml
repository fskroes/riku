[package]
name = "sessions"
version.workspace = true
edition.workspace = true
license.workspace = true
# Workspace-internal. Its public surface (Projection, SessionStore, the Fold trait)
# is shaped for the board and the Collector, and changes with them — nothing outside
# this workspace is meant to pin it.
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }
notify = "6"
dirs = "5"

[dev-dependencies]
tempfile = "3"
